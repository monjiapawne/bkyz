[project]
name = "bkyz"
version = "0.1.0"
requries-python = ">=3.13"
dependencies = [
    "flask",
    "flask_sqlalchemy",
    "flask_migrate",
    "requests",
    "spectree",
]

[tool.ruff]
line-length = 120

[dependency-groups]
test = [
    "pytest"
]
